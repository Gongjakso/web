const windowSize = {
};

const fontSize = {
    xs: '0.5rem',
    sm: '0.75rem',
    base: '1rem',
    md: '1.25rem',
    lg: '1.5rem',
};

const lightMode = {

};

const repo = {

};

const theme = {
    windowSize,
    repo,
    fontSize,
    ...lightMode,
};

export default theme;
